// Importaciones de react (hooks)
import React from "react";
import ReactDOM from "react-dom/client";

// Importaciones de librerias externas (react-query, axios)

// Importaciones internas (componentes, utils, tec)

import { App } from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
